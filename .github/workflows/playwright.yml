name: CI End2End

on:
    pull_request:
        branches:
            - dev

jobs:
  chromium:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Installation des dépendances
        run: |
          cd playwright
          npm ci

      - name: Installation des navigateurs de Playwright
        run: |
          cd playwright
          npx playwright install chromium

      - name: Create backend .env with required variables
        run: |
          cd backend
          echo "JWT_SECRET_KEY=ci-test-secret-key" >> .env
          echo "RESEND_API_KEY=test-api-key" >> .env
          echo "RESEND_EMAIL_DOMAIN=resend.dev" >> .env

      - name: Create frontend .env with required variables
        run: |
          cd frontend
          echo "VITE_ENVIRONMENT=DEV" >> .env
          echo "VITE_STRIPE_PUBLISHABLE_KEY=pk_test_dummy" >> .env

      - name: Set up Docker
        run: |
          docker --version

      - name: Build Docker containers
        run: |
          docker compose up --build -d
        continue-on-error: false

      - name: Wait for database to be ready
        run: |
          echo "Waiting for database to be ready..."
          timeout 60 bash -c 'until docker compose exec -T db pg_isready -U postgres; do sleep 2; done'
          echo "Database is ready!"

      - name: Wait for backend to be ready
        run: |
          echo "Waiting for backend to be ready..."
          timeout 60 bash -c 'until docker compose exec -T backend curl -f http://localhost:4000 > /dev/null 2>&1; do sleep 2; done'
          echo "Backend is ready!"

      - name: Wait for frontend to be ready
        run: |
          echo "Waiting for frontend to be ready..."
          timeout 60 bash -c 'until docker compose exec -T frontend curl -f http://localhost:5173 > /dev/null 2>&1; do sleep 2; done'
          echo "Frontend is ready!"

      - name: Wait for nginx gateway to be ready
        run: |
          echo "Waiting for nginx gateway to be ready..."
          timeout 60 bash -c 'until curl -f http://localhost:7007 > /dev/null 2>&1; do sleep 2; done'
          echo "Nginx gateway is ready!"

      - name: Additional wait for app stability
        run: |
          echo "Waiting additional time for app stability..."
          sleep 10

      - name: Exécution des tests Playwright - Chromium
        run: |
          cd playwright
          npm run test:chromium

      - name: Stop Docker containers
        if: always()
        run: |
          docker compose down -v
          docker system prune -f

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-chromium
          path: playwright/playwright-report/
          retention-days: 30

  firefox:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*

    - name: Installation des dépendances
      run: |
        cd playwright
        npm ci

    - name: Installation des navigateurs de Playwright
      run: |
        cd playwright
        npx playwright install firefox

    - name: Create backend .env with required variables
      run: |
        cd backend
        echo "JWT_SECRET_KEY=ci-test-secret-key" >> .env
        echo "RESEND_API_KEY=test-api-key" >> .env
        echo "RESEND_EMAIL_DOMAIN=resend.dev" >> .env

    - name: Create frontend .env with required variables
      run: |
        cd frontend
        echo "VITE_ENVIRONMENT=DEV" >> .env
        echo "VITE_STRIPE_PUBLISHABLE_KEY=pk_test_dummy" >> .env

    - name: Set up Docker
      run: |
        docker --version

    - name: Build Docker containers
      run: |
        docker compose up --build -d
      continue-on-error: false

    - name: Wait for database to be ready
      run: |
        echo "Waiting for database to be ready..."
        timeout 60 bash -c 'until docker compose exec -T db pg_isready -U postgres; do sleep 2; done'
        echo "Database is ready!"

    - name: Wait for backend to be ready
      run: |
        echo "Waiting for backend to be ready..."
        timeout 60 bash -c 'until docker compose exec -T backend curl -f http://localhost:4000 > /dev/null 2>&1; do sleep 2; done'
        echo "Backend is ready!"

    - name: Wait for frontend to be ready
      run: |
        echo "Waiting for frontend to be ready..."
        timeout 60 bash -c 'until docker compose exec -T frontend curl -f http://localhost:5173 > /dev/null 2>&1; do sleep 2; done'
        echo "Frontend is ready!"

    - name: Wait for nginx gateway to be ready
      run: |
        echo "Waiting for nginx gateway to be ready..."
        timeout 60 bash -c 'until curl -f http://localhost:7007 > /dev/null 2>&1; do sleep 2; done'
        echo "Nginx gateway is ready!"

    - name: Additional wait for app stability
      run: |
        echo "Waiting additional time for app stability..."
        sleep 10

    - name: Exécution des tests Playwright - Firefox
      run: |
        cd playwright
        npm run test:firefox

    - name: Stop Docker containers
      if: always()
      run: |
        docker compose down -v
        docker system prune -f

    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report-firefox
        path: playwright/playwright-report/
        retention-days: 30

  webkit:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Installation des dépendances
        run: |
          cd playwright
          npm ci

      - name: Installation des navigateurs de Playwright
        run: |
          cd playwright
          npx playwright install webkit

      - name: Create backend .env with required variables
        run: |
          cd backend
          echo "JWT_SECRET_KEY=ci-test-secret-key" >> .env
          echo "RESEND_API_KEY=test-api-key" >> .env
          echo "RESEND_EMAIL_DOMAIN=resend.dev" >> .env
          echo "DATABASE_URL=postgresql://postgres:example@db:5432/postgres" >> .env
          echo "NODE_ENV=test" >> .env

      - name: Create frontend .env with required variables
        run: |
          cd frontend
          echo "VITE_ENVIRONMENT=DEV" >> .env
          echo "VITE_STRIPE_PUBLISHABLE_KEY=pk_test_dummy" >> .env

      - name: Set up Docker
        run: |
          docker --version

      - name: Build Docker containers
        run: |
          docker compose up --build -d
        continue-on-error: false

      - name: Wait for database to be ready
        run: |
          echo "Waiting for database to be ready..."
          timeout 60 bash -c 'until docker compose exec -T db pg_isready -U postgres; do sleep 2; done'
          echo "Database is ready!"

      - name: Wait for backend to be ready
        run: |
          echo "Waiting for backend to be ready..."
          timeout 60 bash -c 'until docker compose exec -T backend curl -f http://localhost:4000 > /dev/null 2>&1; do sleep 2; done'
          echo "Backend is ready!"

      - name: Wait for frontend to be ready
        run: |
          echo "Waiting for frontend to be ready..."
          timeout 60 bash -c 'until docker compose exec -T backend curl -f http://localhost:5173 > /dev/null 2>&1; do sleep 2; done'
          echo "Frontend is ready!"

      - name: Wait for nginx gateway to be ready
        run: |
          echo "Waiting for nginx gateway to be ready..."
          timeout 60 bash -c 'until curl -f http://localhost:7007 > /dev/null 2>&1; do sleep 2; done'
          echo "Nginx gateway is ready!"

      - name: Additional wait for app stability
        run: |
          echo "Waiting additional time for app stability..."
          sleep 10

      - name: Exécution des tests Playwright - WebKit
        run: |
          cd playwright
          npm run test:webkit

      - name: Stop Docker containers
        if: always()
        run: |
          docker compose down -v
          docker system prune -f

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-webkit
          path: playwright/playwright-report/
          retention-days: 30